class Solution{
public:
    void Reverse(stack<int> &St){
        vector<int>arr;
        while(!St.empty()){
            arr.push_back(St.top());
            St.pop();
        }
        for(int i=0;i<arr.size();i++){
            St.push(arr[i]);
        }
    }
};
